{
  "name": "basic",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "axios": "^1.4.0"
  }
}
